.Rproj.user
.Rhistory
.Rdata
.httr-oauth
.DS_Store
.quarto

/.quarto/

.Rprofile

**/*.quarto_ipynb

submissions/

no_git

**/data/
**/reviews/

presentations/

manuscript*

call_for_collaboration/

emails/

# Contains CloudResearch participant IDs + the relocated quality report
# (which lists excluded participants). Must never reach the public repo.
cloudresearch/
# Quarto freeze cache for the above (participant-derived chunk output) —
# _freeze/ is otherwise committed, so this exclusion is required.
_freeze/cloudresearch/

.positai

analysis*

# Slides reveal study results; teams must stay blind. Kept local only,
# removed from the public site. Re-add when results can be published.
slides/
docs/slides/
# Freeze cache holds rendered slide result-figure PNGs — exclude even though
# _freeze/ is otherwise committed.
_freeze/slides/

CLAUDE.md
